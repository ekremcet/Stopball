<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ekremcetinkaya.stopball.anaSayfa">

    <com.google.android.gms.ads.AdView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        ads:adSize="BANNER"
        ads:adUnitId="@string/banner_ad_unit_id"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/reklam">
    </com.google.android.gms.ads.AdView>
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/chronoBall"
        android:src="@drawable/chronoduz"
        android:layout_marginTop="40dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Play"
        android:id="@+id/playButton"
        android:layout_below="@+id/chronoBall"
        android:layout_centerHorizontal="true"
        android:onClick="playButton"
        android:background="@android:color/transparent"
        android:textColor="#012c3f"
        android:textSize="20sp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="How to Play"
        android:id="@+id/howToButton"
        android:layout_below="@+id/playButton"
        android:layout_centerHorizontal="true"
        android:onClick="howToButton"
        android:background="@android:color/transparent"
        android:textColor="#012c3f"
        android:textSize="20sp" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EXIT"
        android:id="@+id/exitButton"
        android:layout_below="@+id/howToButton"
        android:layout_centerHorizontal="true"
        android:onClick="exitButton"
        android:background="@android:color/transparent"
        android:textColor="#a90a0a"
        android:textSize="20sp" />

</RelativeLayout>
